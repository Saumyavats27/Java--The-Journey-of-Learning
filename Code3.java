// Date:13-03-2024 ,This is the journey of learning
//CodeWithSaumya: Code3
//The if statement 

public class Code3 {
    public static void main(String[] args) {
        int x, y; // Declare two variable ,by use if a comma-seperated list
        x = 04;
        y = 28;
        if (x < y)
            System.out.println("x is less than y");
        x = x * 7;
        if (x == y)
            System.out.println("X is now equal to y");// If this condition is not true than it will not print anything
        x = x * 2;
        if (x > y)
            System.out.println("x is now greater than y");

        if (x == y)
            System.out.println("You won't see this");

    }
}
